# ----------------------------------------------------------------------------
# INFRASTRUCTURE SETUP (Used by Makefile & Ansible)
# ----------------------------------------------------------------------------
# The linux username on the target server (e.g., 'pi' or your user)
USERNAME=your_linux_username

# ----------------------------------------------------------------------------
# BACKUP APPLICATION CONFIGURATION (Used by Docker & Python)
# ----------------------------------------------------------------------------
# Comma-separated list of Google Drive folders you want to backup
REMOTE_FOLDERS=Documents,Photos,Finance

# Gmail Credentials for Notifications
# Note: Use an "App Password" if you have 2FA enabled on your Google Account
GMAIL_USERNAME=your.email@gmail.com
GMAIL_PASSWORD=your_app_specific_password
GMAIL_RECEIVER=alert.receiver@gmail.com

# Email Notification Toggle (1 = On, 0 = Off)
SEND_EMAIL=1

# ----------------------------------------------------------------------------
# ADVANCED / OPTIONAL
# ----------------------------------------------------------------------------
# Directories for processing (Defaults set in app, override if needed)
# TEMP_FILES_FOLDER=../tempbackups
# BACKUP_FOLDER=../backups